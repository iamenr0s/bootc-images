# Image-shipped SSH policy. sshd uses the first value it reads, and this file
# sorts before 50-redhat.conf and cloud-init's 50-cloud-init.conf, so these win.
PermitRootLogin no
PasswordAuthentication no
KbdInteractiveAuthentication no
PubkeyAuthentication yes
